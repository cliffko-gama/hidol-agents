/**
 * hidol Feature Story — 範例文章（Few-Shot Reference）
 *
 * 把你認為品質符合標準的文章貼在這裡。
 * Agent C 會把它們當作「風格參考」，Agent D 會當作「評分基準」。
 *
 * ─────────────────────────────────────────────────────────────
 * 如何新增範例？
 *
 *   1. 把一篇你滿意的 Feature Story 的「完整 Markdown 文字內容」
 *      貼到下方 EXAMPLE_ARTICLES 陣列裡。
 *
 *   2. 每篇文章是一個物件：
 *      {
 *        title:    "文章標題（10-25 字）",
 *        subtitle: "副標題（15-30 字）",   // 可選
 *        content:  `...Markdown 全文...`,
 *      }
 *
 *   3. 建議放 2-3 篇，太多會增加 API token 消耗。
 *
 *   4. 範例越貼近「你心目中的理想品質」越好——
 *      Agent C 會模仿它的語氣、節奏和 Theme Spine 的使用方式；
 *      Agent D 會以它作為評分基準。
 * ─────────────────────────────────────────────────────────────
 */

export interface ExampleArticle {
  /** 文章標題 */
  title: string;
  /** 副標題（可選） */
  subtitle?: string;
  /** 完整 Markdown 文章內容（sections 拼接後的全文） */
  content: string;
}

/**
 * 範例文章清單
 *
 * 目前為空，等待你填入實際範例。
 * 填好之後，Agent C 和 D 就能在每次撰寫/審核時參照這些範本。
 */
export const EXAMPLE_ARTICLES: ExampleArticle[] = [
  // ── 範例 1 ───────────────────────────────────────────────────
  // {
  //   title: "等了兩年，終於在現場看見他們",
  //   subtitle: "FEniX 見面會現場直擊：那些隔著螢幕永遠感受不到的溫度",
  //   content: `
  // 如果你是 FEniX 的粉，一定有過這樣的時刻——
  // 螢幕上跳出「恭喜中籤」，心臟被狠狠撞了一下。
  //
  // 這篇文章記錄的，就是那天現場的樣子。
  //
  // ## 排隊的人，都帶著一件事
  //
  // 粉絲_ae5bd108 在 Moment 上寫道：
  // > 「等了兩個小時才進場，但當 MAX 轉頭對著我這區笑的時候，什麼疲勞都消失了 🥹」
  //
  // 兩個小時。不是兩分鐘。整整兩個小時。
  // 但她說，值得。
  //
  // [... 繼續貼入完整文章 ...]
  //   `,
  // },

  // ── 範例 2 ───────────────────────────────────────────────────
  // {
  //   title: "把偶像的名字繡在上面，這就是手作應援的意義",
  //   subtitle: "當三萬個粉絲各自做了一件一模一樣的事，那就不是巧合，那是愛",
  //   content: `...`,
  // },
];
